# site/ — sochi.estate

Astro → Cloudflare Pages (0 ₽). Собирается на Этапе 3 промпта (Часть XII канона).

Три закона дизайна (Часть X): объект — герой (тёмная глубина, янтарный акцент, сканирующие кольца — единственная анимация); данные — эстетика (страница объекта = «тикер»); одно решение за раз. Никаких стоковых фото.

Ключевые страницы: главная-хаб · /passport (продающая страница клина) · страницы объектов с блоком Паспорта доверия.

---

**Манифест системы живёт не здесь, а в `/aura` в корне репозитория** (`www.sochi.estate/aura`).
Так пришлось сделать потому, что GitHub Pages публикует корень репозитория целиком:
папка в `site/` открывалась бы по адресу `/site/aura`. Копии страницы нет — файл один.
