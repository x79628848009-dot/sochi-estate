# SITE-AURA — журнал страницы-манифеста

Не объект недвижимости, а публичная поверхность системы: `/aura` → `sochi.estate/aura`.
Формат событий тот же, что у объектов (см. `journal/README.md`) — append-only, новые вниз.

---

## 2026-08-04 14:57 · source: founder · type: publication
Собран одностраничный манифест системы. Чистый HTML/CSS/JS, ноль зависимостей и ноль
сборки, вес страницы 40,6 КБ, фотографий нет — вся графика CSS/SVG. Тексты канонические,
все 42 фрагмента сверены с исходником автоматически. Выбран путь публикации без DNS:
папка `/aura` в корне репозитория, адрес `www.sochi.estate/aura`. Поддомен
`aura.sochi.estate` отложен: GitHub Pages держит один домен на репозиторий, а прав на
создание второго репозитория у обвязки нет.
Открыт PR #1 — слияние = публикация (human-gate, закон № 4).
confidence: high · основание: brief основателя от 2026-08-04, канон Раздел 0.3 и Часть X

## 2026-08-04 15:40 · source: founder · type: correction
Ревизия № 1 по документу `SITE_REVISION_1_FINAL.md`, девять правок, все утверждены
основателем. Существенное:

- H1 стал именем системы — `AURA`, лёгкий вес и разреженный трекинг; прежний заголовок
  «Объект недвижимости ожил» снят, под именем появилась подпись «Объект недвижимости
  говорит сам за себя.». Заголовки `<title>`/OG/twitter приведены к ней же.
- Статус «система строится» проговорён трижды: надбровье героя, заголовок терминала
  («демонстрация»), подпись под ним («так система будет вести журнал каждого объекта»).
- Добавлен блок «Почему этого ещё никто не сделал» между блоком 5 и «Почему Сочи»:
  оркестровка, встроенный маркетинговый движок, обещание про надстройки вполголоса.
- Разгадка блока 5 получила финальный аккорд отдельной строкой: «Между человеком и
  объектом — больше никого.»
- Новый знак: объект в разомкнутом кольце ауры. Кольца оставлено одно — на 16 px два
  сливались в пятно (проверено рендером 16/24/32/64/140). `og.png` пересобран в той же
  концепции, 58,7 КБ.
- Радар отзывается в журнале: на вспышке цели маркер строки загорается на 2 секунды.
  Фаза берётся у самой анимации луча, поэтому подсветка не разъезжается со вспышкой —
  замер подтвердил совпадение с точностью ~0,1 с.
- Контакт кнопки: `michael@rovensky.ru`. Прежний временный адрес удалён из кода, поиском
  по папке не находится.
- Последняя строка страницы: «Это первая страница журнала. Остальное система напишет сама.»
- Карта репозитория в корневом README: строка про `site/` заменена строкой про `/aura`.

Проверено после правок: ширины 320/390/834/1440 — горизонтального переполнения нет
нигде, ошибок в консоли нет, вес страницы 46,8 КБ, при `prefers-reduced-motion` движение
выключается полностью и содержимое остаётся целым.

Не сделано намеренно: папка `site/` оставлена на месте — в ревизии сказано удалять её
только если пустая, а там лежит README с планом основного сайта.
confidence: high · основание: `SITE_REVISION_1_FINAL.md`, автоматические проверки текстов и вёрстки

## 2026-08-04 21:30 · source: founder · type: correction
Знак стал единым на весь домен. Корневой `favicon.svg` (главная страница
sochi.estate) заменён на тот же знак, что у манифеста, — геометрия совпадает
с `aura/favicon.svg` до символа. Прежний корневой знак был старым радаром
с ярким кремовым ядром: на 16 пикселях кольца и ядро сливались в светлое пятно
на тёмном квадрате — основатель увидел это на вкладке и назвал вещи своими
именами.

Причина оказалась не в рисунке, а в подложке: на 16 пикселях плитка —
единственное, что достаточно велико, чтобы её разглядеть, поэтому любой знак
на плитке читается как квадрат. Ошибка проверки с моей стороны: я сверяла
знаки только на тёмном фоне, где тёмная подложка сливается с ним и проблема
не видна. Панель вкладок у основателя светлая.

Кроме `favicon.svg` на главной странице не тронуто ничего: `index.html`
и `CNAME` не изменялись.
confidence: high · основание: скриншот вкладки от основателя, рендер знаков на 16/24/48 на светлом, белом и тёмном фоне

## 2026-08-06 17:49 · source: founder · type: publication
Ревизия № 1 и единый знак домена доставлены на sochi.estate. Выкладка прошла
в 17:49 (коммит `a52febf`, `Reported success!`). На странице теперь заголовок
`AURA`, надбровье «система строится», блок «Почему этого ещё никто не сделал»,
контакт michael@rovensky.ru, знак без подложки — он же на главной.

**Инцидент доставки, стоит запомнить.** Между слиянием и появлением правок на
сайте прошло два с половиной часа, и всё это время посетитель видел версию от
4 августа при полностью корректном коде в ветке. Хронология:

- 16:21 слит PR № 2 → выкладка началась;
- 16:27 слит PR № 3 → GitHub отменил незавершённую выкладку и начал новую;
- 16:28–16:38 очередь Pages не приняла её: десять минут `deployment_queued`,
  прерывание по лимиту;
- 16:39, 16:43 два перезапуска отбиты мгновенно с `Deployment cancelled` —
  номер выкладки равен номеру коммита, а для отменённого номера новая не заводится;
- 17:00 пустой коммит `9f7c4e9` со свежим номером — снова десять минут и лимит;
- в настройках Pages при этом висела незавершённая проверка домена
  (DNS Check in Progress) — тот же сбой, вид сбоку;
- 17:37 основатель переключил ветку публикации туда-обратно, 17:39 пустой
  коммит `a52febf` — выкладка прошла.

**Выводы на будущее.**
1. Не сливать два PR подряд с малым интервалом: второе слияние убивает
   незавершённую выкладку первого и способно заклинить очередь.
2. «Слито» не равно «опубликовано». Признак доставки — зелёный `deploy`
   в pages build and deployment, а не зелёная кнопка Merge.
3. Перезапуск упавшей выкладки бесполезен: нужен новый коммит, а если и он
   не помогает — переключение ветки публикации в настройках Pages.
4. Проверить факт публикации из среды обвязки нельзя: сетевая политика не
   пускает ни на sochi.estate, ни на githubstatus.com. Единственный доступный
   признак — статус задания deploy через API. Это ограничение, а не мелочь:
   визуальную проверку живой страницы делает основатель.
confidence: high · основание: логи заданий deploy, статусы запусков pages build and deployment

## 2026-08-06 18:40 · source: founder · type: correction
В блок «Мы дали объекту то, чего у него никогда не было» добавлен пятый пункт —
**«Право действовать.»**: «Он не ждёт команды на каждый шаг: сам решает, что
делать дальше, и делает. Необратимое остаётся за вами — цена, публикация,
любое данное слово.»

Зачем. Четыре прежних дара давали объекту голову, память, речь и план, но не
действие — получался мыслитель, а не деятель, и у читателя оставался незакрытый
вопрос «оно само что-то делает?». Пункт стоит последним, на нарастание.

Формулировка рассчитана на два уха сразу, по просьбе основателя: технический
читатель видит автономный цикл с подтверждением необратимых действий,
инвестор — автономность с сохранённым контролем. Слов «ReAct» и
«human in the loop» на странице нет сознательно: первое ничего не значит
для покупателя, второе само признаётся, что есть чего бояться.

Слово «Право действовать» выбрано вместо «Инициатива» или «Воля» потому, что
само говорит: право *дано*, а значит имеет границы. Испуг снимается
формулировкой, а не оправданием после неё. Границы названы по канону
(закон № 4): цена, публикация, данное слово.

Вместе с пунктом обновлён лид первого экрана — он дословно разворачивается
этим списком, и без правки возникло бы расхождение: «У объекта есть цель,
память, голос, стратегия и право действовать». Лид меняется в четырёх местах
сразу: текст героя, meta description, og:description, twitter:description.

Колонка терминов в списке расширена с 12 до 15 rem: «Право действовать.»
переносилось на две строки и ломало ритм однострочных соседей.

Проверено: 320/390/834/1440 — переполнения нет, ошибок в консоли нет,
вес страницы 48,3 КБ.
confidence: high · основание: решение основателя от 2026-08-06, канон закон № 4 (human-gates)
